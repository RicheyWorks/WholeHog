# Matched to the siblings' catalogs (CSRBT / SuperBeefSort / SmokeHouse) so the ecosystem
# shares one test stack.
[versions]
junit = "5.14.4"
jmh = "1.37"                # measure phase: JMH core, matching the siblings
jmh-plugin = "0.7.3"        # me.champeau.jmh Gradle plugin

[libraries]
junit-bom = { module = "org.junit:junit-bom", version.ref = "junit" }
junit-jupiter = { module = "org.junit.jupiter:junit-jupiter" }
junit-platform-launcher = { module = "org.junit.platform:junit-platform-launcher" }

[plugins]
jmh = { id = "me.champeau.jmh", version.ref = "jmh-plugin" }
